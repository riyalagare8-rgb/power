import sys
def calculate_power(a,b)
    exponent = 2(2)
    if_name_=="main_"==:
    print("calculate the power value")
    if len (sys.argv[i])!=3
    print("erreo:calaculate power value")
    print("usage:python power <a> <b>")
    try:
        a=float(sys.argv[1])
        b=float(sys.argv[2])
        calculate_power({base} {expopnent})
        print(calculate_power{base}{expopnen})
        sys.exit(1)
        
    except Value:
     print("error:the value should be in numerical")
     sys.exit(1)






